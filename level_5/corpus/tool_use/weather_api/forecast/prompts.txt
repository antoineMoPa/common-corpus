daily_forecast.corpus Domain: weather_api/forecast | Available Tools: get_forecast_daily | get_forecast_hourly | get_forecast_weekly | Call Schema: { "func_call": "string", "location": "string", "days": number, "unit": "string" } | Response Schema: { "location": "string", "forecast": [{ "date": "string", "high": number, "low": number, "condition": "string" }] } |  | Generate 12 tool-use examples for 3-7 day weather forecasts. Use real cities. Responses should include date, high/low temps, and weather condition strings.
hourly_forecast.corpus Domain: weather_api/forecast | Available Tools: get_forecast_daily | get_forecast_hourly | get_forecast_weekly | Call Schema: { "func_call": "string", "location": "string", "days": number, "unit": "string" } | Response Schema: { "location": "string", "forecast": [{ "date": "string", "high": number, "low": number, "condition": "string" }] } |  | Generate 12 tool-use examples for hourly weather forecasts. Use realistic time ranges and include precipitation probability.
