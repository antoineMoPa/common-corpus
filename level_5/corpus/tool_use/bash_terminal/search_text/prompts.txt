grep_basic.corpus Domain: bash_terminal/search_text | Available Tools: grep_file | grep_recursive | grep_case_insensitive | grep_count | Call Schema: { "func_call": "string", "pattern": "string", "path": "string", "flags": "string" } | Response Schema: { "matches": [{ "file": "string", "line": number, "text": "string" }], "count": number } |  | Generate 12 tool-use examples for grepping text in files. Use realistic search patterns (error, TODO, import, password, hostname) across log files and source code.
grep_recursive.corpus Domain: bash_terminal/search_text | Available Tools: grep_file | grep_recursive | grep_case_insensitive | grep_count | Call Schema: { "func_call": "string", "pattern": "string", "path": "string", "flags": "string" } | Response Schema: { "matches": [{ "file": "string", "line": number, "text": "string" }], "count": number } |  | Generate 12 tool-use examples for recursive grep across directories. Include -i case-insensitive, -r recursive, -n line numbers. Use /var/log, ~/projects, /etc.
