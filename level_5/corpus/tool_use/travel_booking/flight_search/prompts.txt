one_way_roundtrip.corpus Domain: travel_booking/flight_search | Available Tools: search_flights | get_flight_details | filter_flights | get_seat_map | Call Schema: { "func_call": "string", "origin": "string", "destination": "string", "departure_date": "string", "return_date": "string", "passengers": number, "cabin": "economy|business|first" } | Response Schema: { "flights": [{ "flight_number": "string", "airline": "string", "departure": "string", "arrival": "string", "price": number, "duration_minutes": number }], "total": number } |  | Generate 12 tool-use examples for searching one-way and round-trip flights. Use real airport codes (JFK, LHR, CDG, NRT) and realistic prices.
cabin_filters.corpus Domain: travel_booking/flight_search | Available Tools: search_flights | get_flight_details | filter_flights | get_seat_map | Call Schema: { "func_call": "string", "origin": "string", "destination": "string", "departure_date": "string", "return_date": "string", "passengers": number, "cabin": "economy|business|first" } | Response Schema: { "flights": [{ "flight_number": "string", "airline": "string", "departure": "string", "arrival": "string", "price": number, "duration_minutes": number }], "total": number } |  | Generate 12 tool-use examples for filtering flights by cabin class and preferences. Include business, economy, direct flight, and stopover filters.
